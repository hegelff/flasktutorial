1. Instruction for Gunicorn Deployment:
Create a virtualenv, install your application, then install gunicorn.

$ cd hello-app
$ python -m venv .venv
$ . .venv/bin/activate
$ pip install .  # install your application
$ pip install gunicorn

2. Run app using Gunicorn: The syntax is {module_import}:{app_variable}. module_import is the dotted import name to the module with your application. app_variable is the variable with the application. It can also be a function call (with any arguments) if you’re using the app factory pattern.

# equivalent to 'from hello import app'
$ gunicorn -w 4 'hello:app'

# equivalent to 'from hello import create_app; create_app()'
$ gunicorn -w 4 'hello:create_app()'

The -w option specifies the number of processes to run; a starting value could be CPU * 2. The default is only 1 worker, which is probably not what you want for the default worker type.

3. Ask AI: how to deploy the official flask tutorial example using docker

To deploy the Flaskr tutorial with Gunicorn and Nginx, you should use Docker Compose. This setup separates the web server (Nginx) from the application server (Gunicorn) for better performance and security.
## 1. Update Project Files
Ensure your project structure includes these two configuration files:
requirements.txt

Flask
gunicorn

nginx.conf
Create this file to tell Nginx how to pass traffic to Flask.

server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass http://flask_app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

------------------------------
## 2. Create the Dockerfile
This builds your Flask application environment.

FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# Initialize the SQLite database
RUN flask --app flaskr init-db
# Run Gunicorn on port 8000
CMD ["gunicorn", "--bind", "0.0.0.0:8000", "flaskr:create_app()"]

------------------------------
## 3. Create the docker-compose.yml
This connects the two containers and manages the network.

services:
  flask_app:
    build: .
    volumes:
      - flask_data:/app/instance

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - flask_app
volumes:
  flask_data:

------------------------------
## 4. Deploy the Stack
Run this single command in your terminal:
docker-compose up --build

🚀 Access your app at: http://localhost
------------------------------
## 🛡️ Why this setup?

* Gunicorn: A robust WSGI HTTP Server that handles multiple simultaneous requests via workers.
* Nginx: Acts as a Reverse Proxy. It is much faster at serving static files (CSS/Images) and provides a layer of protection.
* Volumes: The flask_data volume ensures your SQLite database survives even if you stop or delete the containers.

------------------------------
Advanced Options
If you want to move closer to a real production environment, I can show you how to:

* Add SSL/HTTPS using Let's Encrypt.
* Optimize Gunicorn worker counts based on your CPU.
* Set up environment variables for secret keys and passwords.

Which of these production features should we tackle next?


